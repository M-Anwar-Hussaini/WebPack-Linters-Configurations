{
  "name": "01.-webpack-linters-configurations",
  "version": "1.0.0",
  "description": "1. create a folder by the name of `src`. 2. insdie `src` add at least these files: `index.js`, `index.html` and `style.css`. 3. Run the following command to create `package.json` file: ```sh npm init -y ``` 4. Run following command to install webpack: ```sh npm install webpack webpack-cli --save-dev ``` 5.",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "webpack serve --open",
    "build": "webpack"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "main": "webpack.config.js",
  "devDependencies": {
    "css-loader": "^6.8.1",
    "html-webpack-plugin": "^5.5.2",
    "style-loader": "^3.3.3",
    "webpack": "^5.86.0",
    "webpack-cli": "^5.1.4",
    "webpack-dev-server": "^4.15.1"
  }
}
